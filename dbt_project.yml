
# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'ethp'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'ethp'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

# directories to be removed by `dbt clean`
clean-targets: 
  - "target"
  - "dbt_packages"


flags: 
  fail_fast: true


seeds:
  +database: seeds
  +schema: static

# Configuring models
# Full documentation: https://docs.getdbt.com/docs/configuring-models

# These settings can be overridden in the individual model #}
# files using the `{{ config(...) }}` macro.
models:
  ethp:
    +materialized: table
    +tags: ['eth', 'crypto']
    marts:
      +tags:
        - powerbi
        - report
      +schema: marts

vars:
#  token_name_var: 'tron'
#  token_address_var: '0x50327c6c5a14dcade707abad2e27eb517df87ab5'
#  token_decimals_var: '6'
  token_name_var: 'link'
  token_address_var: '0x514910771af9ca656af840dff83e8264ecf986ca'
  token_decimals_var: '18'


dispatch:
  - macro_namespace: dbt_utils
    search_order: ['ethp', 'dbt_utils']